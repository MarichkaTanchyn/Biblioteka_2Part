<%- include('../../fragments/header.ejs') %>

<script src="../../../public/js/validationEmpForm.js" type="application/javascript"></script>
<script type="application/javascript" src="/js/validationCommon.js"></script>

<h2>
    <%= pageTitle %>
</h2>
<form class="form" method="post" action="<%= formAction %>" novalidate onsubmit="return validateForm();">
    <input type="hidden" name="id" value="<%= dept.id %>">

    <label for="name">Nazwa:<span class="symbol-required">*</span></label>
    <input type="text" name="name" id="name" placeholder=" 2-60 znaków" value="<%= dept.DeptName %>"
            <%= (formMode === 'showDetails') ? 'disabled' : '' %>/>
    <span id="errorDepartmentName"></span>

    <label for="amountofEmp">Ilość Pracujących: <span class="symbol-required">*</span></label>
    <input type="number" name="amountofEmp" id="amountofEmp" placeholder=" 1-10" value="<%= dept.NumOfWorkers %>"
            <%= (formMode === 'showDetails') ? 'disabled' : '' %>/>
    <span id="errorAmountofEmp"></span>

    <label for="dateOfStart">Data: <span class="symbol-required">*</span></label>
    <input type="date" name="dateOfStart" id="dateOfStart" placeholder=" YYYY-MM-DD" value="<%= dept.DateOfStert %>"
            <%= (formMode === 'showDetails') ? 'disabled' : '' %>/>
    <span id="errorDateOfStart" class="errors-text"></span>

    <% if (formMode != 'showDetails') { %>
        <input type="submit" class="button" value="Dodaj"/>
        <a class="button" href="/departments">Anuluj</a>
    <% } %>
</form>

</form>

<% if (formMode === 'showDetails') { %>
    <a class="button" href="/departments/edit/<%= dept.id %>">Edytuj</a>
<% } %>


<% if (formMode === 'showDetails' || formMode === 'edit') { %>
    <% if (dept.employments.length > 0){ %>
        <h2>
            <%= pageTitle %>
        </h2>
        <table class="table-list">
            <thead>
            <tr>
                <th>Imie</th>
                <th>Nazwisko</th>
                <th>Numer Telefonu</th>
                <th>Data Od</th>
            </tr>
            </thead>
            <tbody>

            <% for(let empl of dept.employments) { %>
                <tr>
                    <td><%= empl.employee.EmpName %></td>
                    <td><%= empl.employee.LastName %></td>
                    <td><%= empl.PhoneNumber %></td>
                    <td><%= empl.DataOd.toISOString().split('T')[0] %></td>

                </tr>
            <% } %>

            </tbody>
        </table>
    <% } %>
    <% if (formMode === 'showDetails') { %>
        <a class="button" href="/departments">Powrót</a>


    <% } %>
<% } %>


<%- include('../../fragments/footer.ejs') %>